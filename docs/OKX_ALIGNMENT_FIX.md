# OKX数据对齐问题修复

## 🔧 问题描述

**症状**：在escape-signal-history页面，OKX 27币种涨跌曲线（紫色）只在图表右侧显示一小段，而不是覆盖整个时间轴（从1月3日开始）。

**原因**：
1. 逃顶信号数据：26835条记录，几乎每分钟都有数据
2. OKX历史数据：311条小时级数据 + 126条分钟级数据 = 437条
3. 原对齐算法：使用精确的分钟级时间匹配（YYYY-MM-DD HH:MM）
4. 结果：只有分钟完全匹配的点才有数据，大部分时间点为null

## ✅ 解决方案

### 修改前的算法
```javascript
// 精确匹配 - 只有分钟完全一致才匹配
const okxDataMap = {};
okxResult.data.forEach(d => {
    const key = `YYYY-MM-DD HH:MM`;  // 精确到分钟
    okxDataMap[key] = d.total_change;
});

result.recent_data.forEach(d => {
    const key = d.stat_time.substring(0, 16);
    okxChangeData.push(okxDataMap[key] || null);  // 大部分为null
});
```

### 修改后的算法
```javascript
// 最近邻插值 - 在30分钟内找最近的数据点
const okxDataArray = okxResult.data.map(d => ({
    timestamp: new Date(d.record_time).getTime(),
    value: d.total_change
})).sort((a, b) => a.timestamp - b.timestamp);

result.recent_data.forEach(d => {
    const targetTime = new Date(d.stat_time).getTime();
    
    let closestData = null;
    let minDiff = Infinity;
    
    for (const okxPoint of okxDataArray) {
        const diff = Math.abs(okxPoint.timestamp - targetTime);
        // 30分钟内的数据点
        if (diff < 30 * 60 * 1000 && diff < minDiff) {
            minDiff = diff;
            closestData = okxPoint.value;
        }
    }
    
    okxChangeData.push(closestData);  // 大部分都有值
});
```

## 📊 效果对比

### 修改前
- 对齐成功的点：~126个（只有分钟级实时数据能精确匹配）
- 覆盖率：126 / 26835 = 0.47%
- 显示效果：只在右侧有一小段紫色曲线

### 修改后
- 对齐成功的点：~26835个（几乎所有时间点都能找到30分钟内的OKX数据）
- 覆盖率：~100%
- 显示效果：紫色曲线覆盖整个时间轴（从1月3日到现在）

## 🎯 技术细节

### 为什么选择30分钟阈值？
1. OKX历史数据是**小时级**的（每小时一个点）
2. 30分钟可以确保每个逃顶信号时间点都能找到附近的OKX数据
3. 不会过度外推（如果用60分钟，可能会出现不合理的插值）

### 数据分布
```
逃顶信号时间轴：
|----o----o----o----o----o----o----o----o----| (每分钟一个点)
     1    2    3    4    5    6    7    8

OKX历史数据：
|----------X--------------------X-----------| (每小时一个点)
          0:00                 1:00

最近邻插值后：
|----X----X----X----X----X----X----X----X----| (每个逃顶点都找到最近的OKX数据)
     ↑                        ↑
   使用0:00的数据          使用1:00的数据
```

## 🌐 验证方法

### 1. 访问页面
https://5000-igsydcyqs9jlcot56rnqk-18e660f9.sandbox.novita.ai/escape-signal-history

### 2. 检查控制台日志
打开浏览器开发者工具（F12），查看控制台输出：
```
📈 OKX涨跌数据已对齐: XXXXX 个点
```
- 修改前：约126个点
- 修改后：约26835个点

### 3. 观察图表
- ✅ 紫色曲线应该从图表左侧（1月3日）延伸到右侧（今天）
- ✅ 曲线应该完整覆盖整个时间轴
- ✅ 与红色（24h信号）和橙色（2h信号）曲线的时间范围一致

## 📝 Git提交

```
commit e278d21
Author: AI Assistant
Date: 2026-01-16

fix: 改进OKX数据对齐算法，使用最近邻插值

- 原算法：精确匹配分钟级时间，导致历史数据点过少
- 新算法：最近邻插值，30分钟内寻找最近数据点
- 效果：OKX曲线将覆盖整个时间轴，而不是只有右侧一小段
```

## 🎉 预期结果

刷新页面后，您应该看到：
1. ✅ 紫色OKX曲线从1月3日开始，覆盖整个时间轴
2. ✅ 曲线连续平滑，没有大段空白
3. ✅ 与逃顶信号曲线的时间范围完全一致
4. ✅ 图例中"OKX 27币种总涨跌%"正常显示

---

**修复状态**：✅ 已完成  
**测试状态**：✅ 等待用户验证  
**文件修改**：source_code/templates/escape_signal_history.html
