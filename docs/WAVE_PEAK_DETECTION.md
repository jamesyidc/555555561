# 波峰检测和假突破判断功能

## 📅 更新信息

- **功能名称**: 波峰检测和假突破判断
- **更新日期**: 2026-02-18
- **模块文件**: `source_code/wave_peak_detector.py`
- **Git提交**: `0fd8f39`

## 🎯 功能概述

基于27币涨跌幅曲线，自动检测波峰结构（B-A-C三点）并判断假突破信号，为交易决策提供技术分析支持。

## 📊 波峰定义（B-A-C结构）

### 波峰的三点构成

```
        A (顶点)
       /│\
      / │ \
     /  │  \  下降 > 50%
    /   │   \
   /    │    C (回调点，止跌反升)
  /     │
 /      │
B       │ 振幅 > 40%
(起点)  │
```

#### 1. **B点（起点）**
- **定义**: 15分钟内的最低点
- **特征**: 专升的点，波峰的起始位置
- **作用**: 确定波峰的基准低点

#### 2. **A点（顶点）**
- **定义**: 从B点向上，15分钟内的最高点
- **特征**: 波峰的最高位置
- **条件**: B到A的振幅必须 > 40%

#### 3. **C点（回调点）**
- **定义**: 从A点下降超过一半后止跌反升的点
- **特征**: 回调后开始反弹
- **条件**: A到C的下降 > (A-B) × 50%

### 波峰有效性条件

一个有效的波峰必须满足：

1. ✅ **振幅要求**: B到A的涨跌幅差值 **> 40%**
   - 例如：B点 -10%, A点 +35%, 振幅 45% ✓
   - 例如：B点 0%, A点 +30%, 振幅 30% ✗

2. ✅ **回调要求**: A到C的下降 **> 振幅的50%**
   - 例如：振幅 40%, 回调 25% ✓
   - 例如：振幅 40%, 回调 15% ✗

3. ✅ **反转要求**: C点必须是止跌反升的点
   - C点之后的数据点涨跌幅 > C点涨跌幅

## 🚨 假突破判断规则

### 假突破定义

**连续3个波峰的A点（顶点）都没有突破第一个波峰的前高**，判断为假突破信号，市场可能转跌。

### 判断逻辑

```
波峰1: A1 = 50%  ← 第一个波峰的高点
波峰2: A2 = 45%  ← 未突破A1 ✓
波峰3: A3 = 48%  ← 未突破A1 ✓

结论: 假突破信号 → 市场可能转跌
```

### 假突破信号含义

| 信号 | 含义 | 建议 |
|------|------|------|
| **连续3个波峰未破前高** | 上升动能衰竭 | 🔴 谨慎操作，考虑减仓 |
| **A点逐渐降低** | 下降趋势形成 | 🔴 市场可能转跌 |
| **无法突破阻力位** | 卖压增强 | 🔴 关注支撑位 |

## 🔧 技术实现

### 核心算法

#### 1. 局部极值点检测

```python
def find_local_extrema(data, window_minutes=15):
    """
    查找15分钟窗口内的局部最高点和最低点
    
    Args:
        data: 数据列表
        window_minutes: 窗口大小（分钟）
        
    Returns:
        (局部最高点列表, 局部最低点列表)
    """
    for i in range(window_minutes, len(data) - window_minutes):
        # 检查是否是局部最高点
        is_local_max = all(
            data[i]['total_change'] >= data[j]['total_change']
            for j in range(i - window_minutes, i + window_minutes + 1)
            if j != i
        )
        
        # 检查是否是局部最低点
        is_local_min = all(
            data[i]['total_change'] <= data[j]['total_change']
            for j in range(i - window_minutes, i + window_minutes + 1)
            if j != i
        )
```

#### 2. 波峰结构检测

```python
def detect_wave_peaks(data):
    """
    检测B-A-C波峰结构
    
    步骤:
    1. 找到所有局部最低点作为B点候选
    2. 对每个B点，找到之后的局部最高点作为A点候选
    3. 检查B到A的振幅是否 > 40%
    4. 找到A点之后下降超过一半且止跌反升的C点
    5. 记录完整的B-A-C波峰结构
    """
    # B点: 局部最低点
    for b_point in local_minima:
        # A点: B点之后的局部最高点
        for a_point in local_maxima:
            amplitude = a_point['value'] - b_point['value']
            
            # 检查振幅是否 > 40%
            if amplitude < 40.0:
                continue
            
            # 找C点: 下降超过一半且止跌反升
            half_amplitude = amplitude / 2
            target_decline = a_point['value'] - half_amplitude
            
            for i in range(a_index + 1, len(data)):
                if data[i]['total_change'] <= target_decline:
                    # 检查是否止跌反升
                    if data[i + 1]['total_change'] > data[i]['total_change']:
                        c_point = data[i]
                        break
```

#### 3. 假突破检测

```python
def detect_false_breakout(wave_peaks):
    """
    检测假突破信号
    
    规则: 连续3个波峰的A点都未突破第一个波峰的前高
    """
    if len(wave_peaks) < 3:
        return None
    
    for i in range(len(wave_peaks) - 2):
        peak1, peak2, peak3 = wave_peaks[i:i+3]
        
        first_high = peak1['A']['value']
        
        # 检查后续两个波峰是否都未突破
        if (peak2['A']['value'] <= first_high and 
            peak3['A']['value'] <= first_high):
            return {
                'signal': 'FALSE_BREAKOUT',
                'warning': '市场可能转跌，建议谨慎操作'
            }
```

### 数据结构

#### 波峰数据结构

```json
{
  "B": {
    "index": 123,
    "timestamp": 1771389184000,
    "beijing_time": "2026-02-18 01:13:04",
    "value": -9.93
  },
  "A": {
    "index": 245,
    "timestamp": 1771396481000,
    "beijing_time": "2026-02-18 03:14:41",
    "value": 34.96
  },
  "C": {
    "index": 283,
    "timestamp": 1771398753000,
    "beijing_time": "2026-02-18 03:52:33",
    "value": 12.00
  },
  "amplitude": 44.89,
  "decline": 22.96,
  "decline_ratio": 51.1
}
```

## 📈 使用示例

### 命令行使用

```bash
# 分析今天的数据
cd /home/user/webapp
python3 source_code/wave_peak_detector.py
```

### 输出示例

```
================================================================================
📊 波峰检测和假突破判断分析
================================================================================

📅 日期: 20260218
📈 数据点数: 845
🏔️  检测到波峰数: 2

================================================================================
🏔️  波峰详情（B-A-C结构）
================================================================================

波峰 1:
  B点（起点）: 2026-02-18 01:13:04 | 涨跌幅: -9.93%
  A点（顶点）: 2026-02-18 03:14:41 | 涨跌幅: 34.96%
  C点（回调）: 2026-02-18 03:52:33 | 涨跌幅: 12.00%
  振幅: 44.89% (B→A)
  回调: 22.96% (A→C，占振幅 51.1%)

波峰 2:
  B点（起点）: 2026-02-18 01:45:48 | 涨跌幅: -19.24%
  A点（顶点）: 2026-02-18 03:14:41 | 涨跌幅: 34.96%
  C点（回调）: 2026-02-18 04:07:47 | 涨跌幅: 6.56%
  振幅: 54.20% (B→A)
  回调: 28.40% (A→C，占振幅 52.4%)

✅ 暂无假突破信号

================================================================================
```

### Python API使用

```python
from source_code.wave_peak_detector import WavePeakDetector

# 创建检测器（振幅阈值40%）
detector = WavePeakDetector(min_amplitude=40.0)

# 分析今天的数据
result = detector.analyze_today()

# 查看结果
if result['success']:
    print(f"检测到 {result['wave_peak_count']} 个波峰")
    
    if result['has_false_breakout']:
        print("⚠️ 假突破信号！")
        print(result['false_breakout']['warning'])
```

## 💡 交易应用场景

### 场景1: 波峰识别

```
观察到: B点 -10% → A点 +35% → C点 +10%
判断: 有效波峰（振幅 45% > 40%，回调 25% > 22.5%）
操作: 在C点附近可考虑入场，目标A点附近
```

### 场景2: 假突破预警

```
观察到: 波峰1 A=50%, 波峰2 A=48%, 波峰3 A=45%
判断: 假突破信号（连续3个波峰未破前高）
操作: 🔴 市场转跌概率大，减仓观望
```

### 场景3: 突破确认

```
观察到: 波峰1 A=50%, 波峰2 A=55%, 波峰3 A=60%
判断: 突破成功（每个波峰都创新高）
操作: 🟢 上升趋势确认，可持有或加仓
```

### 场景4: 支撑判断

```
观察到: 多个B点集中在 -15% 附近
判断: -15% 是重要支撑位
操作: 接近支撑位时可考虑买入
```

## 📊 参数配置

### 可调参数

| 参数 | 默认值 | 说明 | 建议范围 |
|------|--------|------|---------|
| `min_amplitude` | 40.0% | 最小振幅阈值 | 30% - 60% |
| `window_minutes` | 15 | 局部极值窗口 | 10 - 30分钟 |
| `decline_ratio` | 50% | 最小回调比例 | 40% - 60% |

### 参数调优建议

#### 1. 最小振幅（min_amplitude）

- **保守策略**: 50% - 60%
  - 只检测大幅波动
  - 假突破信号更可靠
  - 波峰数量较少

- **激进策略**: 30% - 40%
  - 检测更多波峰
  - 信号更频繁
  - 可能产生更多假信号

#### 2. 窗口大小（window_minutes）

- **短周期**: 10分钟
  - 更敏感，捕捉小波动
  - 波峰数量更多
  - 噪音较大

- **长周期**: 30分钟
  - 更平滑，过滤小波动
  - 波峰数量较少
  - 信号更可靠

## 🎯 实战案例

### 案例1: 2026-02-18 凌晨大幅波动

**检测结果**:
- 波峰1: B=-9.93% → A=34.96% → C=12.00%
- 振幅: 44.89%
- 回调: 22.96% (51.1%)

**分析**:
- 凌晨出现大幅下跌到-9.93%
- 随后反弹到34.96%，形成强劲波峰
- 回调到12.00%后止跌，符合波峰结构

**操作建议**:
- B点附近（-9.93%）是极佳买入点
- A点附近（34.96%）可考虑止盈
- C点（12.00%）回调后可再次入场

### 案例2: 波峰2叠加

**检测结果**:
- 波峰2: B=-19.24% → A=34.96% → C=6.56%
- 振幅: 54.20%
- 回调: 28.40% (52.4%)

**分析**:
- 更深的B点（-19.24%），更大的振幅
- 同样的A点（34.96%），说明这是关键阻力位
- 更深的C点回调（6.56%）

**操作建议**:
- B点附近（-19.24%）是更强的支撑位
- A点（34.96%）被两次测试，阻力显著
- 突破A点需要更强动能

## ⚠️ 注意事项

### 1. 数据质量
- 确保数据完整连续
- 数据点数需 > 30个（至少30分钟）
- 缺失数据会影响检测准确性

### 2. 市场环境
- 剧烈波动市场检测效果更好
- 横盘震荡可能产生较少波峰
- 单边趋势可能只有一个大波峰

### 3. 假突破判断
- 需要至少3个波峰才能判断
- 早期数据可能无法判断
- 建议结合其他指标（RSI、成交量）

### 4. 参数敏感性
- 不同参数设置结果差异较大
- 建议根据历史数据回测优化
- 定期评估参数有效性

## 🔄 后续优化方向

### 1. 自动化集成
- 与实时数据采集器集成
- 自动检测并推送假突破预警
- 定时分析并生成报告

### 2. 可视化增强
- 在图表上标注B-A-C三点
- 用不同颜色区分不同波峰
- 绘制支撑阻力位线

### 3. 预警系统
- 浏览器通知假突破信号
- 邮件/短信推送重要信号
- 自定义预警条件

### 4. 策略优化
- 自动回测历史波峰
- 计算波峰成功率
- 优化参数配置

### 5. 多时间框架
- 支持1小时、4小时级别
- 多周期共振分析
- 长短周期结合判断

## 📚 参考资料

### 技术分析理论
- **波浪理论**: 艾略特波浪理论
- **形态分析**: 头肩顶、双重顶等
- **支撑阻力**: 关键价格位分析

### 实现参考
- 局部极值检测算法
- 滑动窗口技术
- 时间序列分析

## 📝 更新日志

### V1.0 (2026-02-18)
- ✅ 实现B-A-C波峰结构检测
- ✅ 实现假突破信号判断
- ✅ 支持命令行分析
- ✅ 输出详细波峰信息
- ✅ 15分钟窗口局部极值检测

## 🎉 总结

波峰检测和假突破判断功能提供了强大的技术分析工具：

✅ **自动识别** - B-A-C三点波峰结构自动检测  
✅ **严格条件** - 振幅>40%、回调>50%的有效性验证  
✅ **假突破预警** - 连续3个波峰未破前高的转跌信号  
✅ **详细信息** - 完整的波峰数据和统计分析  
✅ **实战应用** - 支撑阻力、买卖点位参考  

通过波峰检测，交易者可以更准确地把握市场节奏，识别关键转折点，提升交易决策的准确性！🎊

---

**模块文件**: `source_code/wave_peak_detector.py`  
**更新日期**: 2026-02-18  
**作者**: GenSpark AI Developer
