# 极值记录自定义排序功能说明

## 功能描述
历史极值记录现在按照指定的币种顺序显示，每个币种显示4条记录（做多最大盈利、做多最大亏损、做空最大盈利、做空最大亏损）。

## 排序顺序

### 币种顺序（共23个）
1. **CFX** - NO.1
2. **FIL** - NO.2
3. **CRO** - NO.3
4. **UNI** - NO.4
5. **CRV** - NO.5
6. **LDO** - NO.6
7. **STX** - NO.7
8. **BCH** - NO.8
9. **SOL** - NO.9
10. **XLM** - NO.10
11. **TAO** - NO.11
12. **APT** - NO.12
13. **TON** - NO.13
14. **HBAR** - NO.14
15. **XRP** - NO.15
16. **NEAR** - NO.16
17. **TRX** - NO.17
18. **DOT** - NO.18
19. **BNB** - NO.19
20. **LINK** - NO.20
21. **DOGE** - NO.21
22. **SUI** - NO.22
23. **AAVE** - NO.23

### 每个币种的记录顺序
对于每个币种，按以下顺序显示4条记录：

1. **做多 最大盈利** (long + max_profit)
2. **做多 最大亏损** (long + max_loss)
3. **做空 最大盈利** (short + max_profit)
4. **做空 最大亏损** (short + max_loss)

### 完整显示顺序示例（前12条）
```
1. CFX 做多 最大盈利
2. CFX 做多 最大亏损
3. CFX 做空 最大盈利
4. CFX 做空 最大亏损
5. FIL 做多 最大盈利
6. FIL 做多 最大亏损
7. FIL 做空 最大盈利
8. FIL 做空 最大亏损
9. CRO 做多 最大盈利
10. CRO 做多 最大亏损
11. CRO 做空 最大盈利
12. CRO 做空 最大亏损
... (依此类推)
```

## 技术实现

### 1. 排序配置
在`renderRecordsTable`函数中定义了`sortOrder`数组，包含92条记录的顺序（23个币种 × 4条记录）：

```javascript
const sortOrder = [
    ['CFX-USDT-SWAP', 'long', 'max_profit'],
    ['CFX-USDT-SWAP', 'long', 'max_loss'],
    ['CFX-USDT-SWAP', 'short', 'max_profit'],
    ['CFX-USDT-SWAP', 'short', 'max_loss'],
    // ... 更多币种
];
```

### 2. 排序逻辑
```javascript
// 创建排序索引映射
const orderMap = {};
sortOrder.forEach((item, index) => {
    const key = `${item[0]}_${item[1]}_${item[2]}`;
    orderMap[key] = index;
});

// 对数据进行排序
const sortedData = [...data].sort((a, b) => {
    const keyA = `${a.inst_id}_${a.pos_side}_${a.record_type}`;
    const keyB = `${b.inst_id}_${b.pos_side}_${b.record_type}`;
    const orderA = orderMap[keyA] !== undefined ? orderMap[keyA] : 999999;
    const orderB = orderMap[keyB] !== undefined ? orderMap[keyB] : 999999;
    return orderA - orderB;
});
```

### 3. 关键字段
- **inst_id**: 交易对，如 `CFX-USDT-SWAP`
- **pos_side**: 持仓方向
  - `long`: 做多
  - `short`: 做空
- **record_type**: 记录类型
  - `max_profit`: 最大盈利
  - `max_loss`: 最大亏损

## 数据显示

### 表格列
| 列名 | 说明 |
|------|------|
| 编号 | 币种序号（NO.1~NO.23） |
| 币种 | 交易对名称 |
| 方向 | 做多/做空 |
| 类型 | 🏆最高盈利/📉最大亏损 |
| 收益率 | 盈亏百分比 |
| 持仓量 | 持仓数量 |
| 开仓价 | 平均开仓价格 |
| 标记价 | 当前标记价格 |
| 时间 | 记录时间戳 |
| 持续时间 | 距现在的时长 |

### 颜色标识
- **做多**: 绿色徽章
- **做空**: 红色徽章
- **最高盈利**: 绿色徽章 + 🏆
- **最大亏损**: 红色徽章 + 📉
- **正收益**: 绿色文字
- **负收益**: 红色文字

## 统计信息

### 1小时内统计
页面顶部显示最近1小时内的统计数据：
- **多单盈利**: 绿色
- **多单亏损**: 红色
- **空单盈利**: 绿色
- **空单亏损**: 红色

## 访问地址
https://5000-igsydcyqs9jlcot56rnqk-8f57ffe2.sandbox.novita.ai/anchor-system-real

## 优势

### 1. 可读性强
- 按币种分组显示
- 每个币种的4种情况一目了然
- 便于比较不同币种的表现

### 2. 一致性
- 固定的排序顺序
- 不受数据更新时间影响
- 便于查找特定币种

### 3. 完整性
- 涵盖所有监控币种
- 包含所有极值情况
- 支持动态更新

## 注意事项

1. **数据完整性**: 如果某个币种没有某种类型的记录（如没有做多的最大盈利），该位置会显示为空或使用默认值

2. **实时更新**: 排序仅在前端渲染时应用，不影响后端数据存储

3. **性能**: 对于大量数据（>100条），排序可能有轻微性能影响

## 代码位置
- **文件**: `source_code/templates/anchor_system_real.html`
- **函数**: `renderRecordsTable(data)`
- **行数**: 约1390-1500行

## Git提交
```
Commit: 7e60fda
Message: feat: 极值记录按照用户指定顺序显示（CFX->FIL->CRO...AAVE，每个币种4条记录）
Files: source_code/templates/anchor_system_real.html
Changes: +155, -13
```

---

**状态**: ✅ 已实现  
**更新时间**: 2026-01-14 21:05  
**影响页面**: anchor-system-real
